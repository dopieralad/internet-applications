import React, {Component} from "react";

class NotFoundPage extends Component {
    render() {
        return (
            <span>Not found page works!</span>
        );
    }
}

export default NotFoundPage;
